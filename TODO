
* aplicar CRUD en controller.js: esta recibiendo la informacion del cliente, falta envairla al servidor y devolver la base de datos correspondiente
* unificar signup/signin: estan usando el mismo codigo linea por linea. Hay que unificar para que la unica diferencia sea la ruta a la que llevan

COMMIT:


TASKS
